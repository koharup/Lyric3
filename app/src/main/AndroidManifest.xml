<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.sano.picchi.lyricmemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="app.sano.picchi.lyricmemo.Translation2Activity" />
        <activity android:name="app.sano.picchi.lyricmemo.TranslationActivity" />
        <activity android:name="app.sano.picchi.lyricmemo.DetailActivity" />
        <activity android:name="app.sano.picchi.lyricmemo.AddActivity" />
        <activity android:name="app.sano.picchi.lyricmemo.MainActivity"></activity>
        <activity android:name="app.sano.picchi.lyricmemo.FirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>